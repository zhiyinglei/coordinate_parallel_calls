# coordinate_parallel_calls

Three functions call1, call2, call3 will be called in parallel, after all of them finish, the callback function will fire to print out the results.


'node client.js' to run the function. 
